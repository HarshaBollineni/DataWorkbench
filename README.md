# DataWorkbench workspace

The application source is in [`source-codes/`](source-codes/). Project-level specifications,
release plans, historical handoffs, and input snapshots are indexed in
[`documentation/README.md`](documentation/README.md).

Supporting source material remains in `dev-requirements/` and `spec-assets/`.

Validate documentation from this directory with `./documentation.ps1 check`.
See the [maintenance policy](documentation/documentation-maintenance.md) for
document ownership and change-impact guidance.
