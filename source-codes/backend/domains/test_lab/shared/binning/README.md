# Shared Information Value and binning

This package owns deterministic fine/coarse binning and Information Value contracts shared by
T1-D02 Feature Target Separation and T4-D14 Population Stability. It is reusable analytical
infrastructure, not an independently registered diagnostic.

Former imports below `dq_diagnostics.engines.feature_target_separation.information_value` remain
compatibility aliases during staged migration.
