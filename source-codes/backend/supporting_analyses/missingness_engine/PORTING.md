# Missingness engine provenance

This domain package was ported from
`C:\Src\missingChecks\backend\src\missing_checks` version `0.2.0` for
DataWorkbench Phase 1B.

The deterministic analysis modules are kept together so upstream fixes can be
compared and applied deliberately. The former FastAPI API, upload ingestion,
job manager, file-reporting adapters and React application were not copied.
DataWorkbench owns snapshot access, manifests, execution, artifacts and review.
