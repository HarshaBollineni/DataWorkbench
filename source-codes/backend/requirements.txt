# Direct dependency baseline tested on Python 3.12.4 / Windows.
# Transitive dependencies and OS-specific wheels still require a release lock.
fastapi==0.141.1
uvicorn[standard]==0.52.2
pandas==3.0.5
pyarrow==25.0.1
numpy==2.5.2
scipy==1.18.0
great-expectations==1.20.0
openai==3.0.0
python-dotenv==1.2.2
python-multipart==0.0.32
openpyxl==3.1.5
pdfplumber==0.11.10
python-docx==1.2.0
fpdf2==2.8.8
statsmodels==0.14.6
scikit-learn==1.9.0
optbinning==0.21.0
pyyaml==6.0.3
rapidfuzz==3.14.6

# dev/test only — not needed at runtime, but ci-local.ps1's "backend: pytest"
# gate requires it, so it lives here rather than a separate file setup.ps1
# doesn't install.
pytest==9.1.1
