/* global __APP_VERSION__ */

// Vite replaces this token at build time with the repository's VERSION value.
// Keeping the display value here prevents release labels from drifting apart.
export const APP_VERSION = __APP_VERSION__;
