[pytest]
# This project does not use pytest's persistent cache fixture or cache-based
# run ordering. Disable the provider because Windows sandbox ACL handling can
# otherwise leave cache/staging directories in the source tree.
addopts = -p no:cacheprovider
pythonpath = backend
