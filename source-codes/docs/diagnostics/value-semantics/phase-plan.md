# Diagnostic 8 — production phase plan

## Phase 0 — alignment and intake

Completed. Confirmed the intended user decision, three-tag vocabulary, general-context behavior, optional PD/LGD/EAD hints, human binding gate, partial `UNSCOPED` coverage, issue/RCA boundary, KB history and AAR storage contract.

## Phase 1 — promote analytical assets

Completed. Carried the Value Semantics v0.1/v0.2 KB, terminology enhancement, matching, engine, summaries, actions, prompt and structured adjudication contract from the experiment. Retained notebooks/fixtures/benchmarks as experiment evidence rather than runtime dependencies.

## Phase 2 — production manifest and service workflow

Completed. Added tenant-aware draft/resume/discard, mandatory information acknowledgement, table/field scope, optional context, role confirmation, declaration validation, coverage preview, immutable freeze, deterministic execution, progress/result routes and failure behavior.

## Phase 3 — reports, artifacts and knowledge publication

Completed. Added authenticated diagnostic reports; JSON bindings/report artifacts; Parquet tag/ledger artifacts; AAR media metadata, hash verification and downloads; Value Semantics KB history; and active terminology v0.3 with v0.2 retained.

## Phase 4 — user interface and action workflow

Completed. Added the four-step D08 Test Lab scope gate, reopenable About page, result/coverage display, report and Parquet downloads, grouped finding disposition, and shared issue/RCA handoff.

## Phase 5 — hardening and enablement

Completed. Corrected frequency windows, structured predicates, ambiguous role cardinality, precedence, sentinel-only partial execution, declaration shapes and unclassified action semantics. Passed the focused acceptance gates and enabled D08 in the framework register.

## Operational follow-up

Monitor runtime and Parquet sizes on representative production snapshots. Treat a future rule/role/precedence change as an explicit KB/methodology version, and use the register-only rollback if launch must be suspended without affecting historical evidence.
