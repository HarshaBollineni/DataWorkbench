# 0.4.0 verification archive

`verify_plan8.py` is the assert-based framework verification gate retained from release 0.4.0.
It is historical evidence rather than the current test entry point. Current behavior is verified
by the domain-aligned pytest suites under `backend/tests`.
