# Archived knowledge-base material

`valuesemantics_kb_combined_legacy.json` is retained only as historical source material. It contains
local-run bindings and legacy identifiers and is deliberately outside the active YAML KB path.
